"""Service helpers that orchestrate persistence around the engine.

These are API-layer orchestration (DB reads/writes around an engine call), not
engine logic — the assembly engine, strategies, and contract are untouched.
"""
